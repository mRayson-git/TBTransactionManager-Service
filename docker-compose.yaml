services:
    web:
        build: .
        container_name: thinkbudget
        ports:
            - "3000:3000"
    mongo:
        image: mongo
        container_name: mongo
        restart: unless-stopped
        ports:
            - "27017:27017"
        volumes:
            - ./database:/data/db